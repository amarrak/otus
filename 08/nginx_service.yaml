apiVersion: v1
kind: Service
metadata:
  name: nginx-service
#  namespace: myapp
  labels:
    pro-monitor
spec:
  selector:
    app: nginx-app
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 80
    name: web
  - port: 9093
    targetPort: 9000
    name: metrics
  type: NodePort
